# Only include packages NOT available in Lambda runtime

# yt-dlp for YouTube video downloads
# RECOMMENDATION: Install yt-dlp as a Lambda LAYER instead of packaging it
# This reduces deployment package size significantly
yt-dlp>=2024.11.18

# Note: boto3 is already available in Lambda runtime - don't package it!
# Note: Requires Node.js and FFmpeg as Lambda layers
