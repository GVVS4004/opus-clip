# Only include packages NOT available in Lambda runtime

# HTTP requests for API calls (Groq, AssemblyAI, Deepgram)
requests>=2.31.0

# OpenAI Whisper for local transcription (optional - only needed if USE_LOCAL_WHISPER=true)
# WARNING: This is a HUGE package (~3GB). Use Lambda Container Images instead of ZIP deployment.
# openai-whisper==20231117
# numpy==1.26.2
# torch>=2.0.0
# torchaudio>=2.0.0

# Note: boto3 and botocore are already available in Lambda runtime - don't package them!
